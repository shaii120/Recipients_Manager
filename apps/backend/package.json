{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "generator_path": "../packages/prisma-generator-zod-local",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist && rm -rf node_modules",
    "dev": "nodemon src/server.ts",
    "prisma:generate": "prisma generate",
    "generator:build": "pnpm --prefix $npm_package_generator_path run build",
    "generator:watch": "pnpm --prefix $npm_package_generator_path run watch",
    "setup": "pnpm install && pnpm run prisma:generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/adapter-mssql": "^7.2.0",
    "@prisma/client": "^7.1.0",
    "@prisma/generator-helper": "^7.1.0",
    "@receipts/shared-schemas": "workspace:*",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "prisma": "^7.1.0",
    "prisma-generator-zod-local": "file:../packages/prisma-generator-zod-local",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^20.0.0",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2"
  }
}